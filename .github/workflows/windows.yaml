name: Windows
defaults:
  run:
    shell: powershell
on:
  workflow_dispatch: 

jobs: 
  build:
    runs-on: windows-latest
    steps:
#     - run: (Get-CimInstance Win32_PageFileUsage).AllocatedBaseSize
    - uses: al-cheb/configure-pagefile-action@v1.2
      with:
        minimum-size: 16GB
        disk-root: "C:"
    - uses: actions/checkout@v2
#     - run: Get-PSDrive
    - run: cinst --no-progress openssh
    - run: |
        scp
        pip install fastcore
        python build.py
