name: Build Binaries
on:
  workflow_dispatch: 

jobs: 
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [macos, ubuntu]
    runs-on: ${{ matrix.os }}-latest
    steps:
    - uses: actions/checkout@v2
    - name: Build Binary
      id: binary
      run: bash build.sh
    - uses: fastai/workflows/rsync@master
      with:
        username: jeremyphoward
        hostname: ps625762.dreamhostps.com
        dest_path: files.fast.ai/hugo/${{ matrix.os }}
        ssh_key: ${{ secrets.DH_KEY }}
